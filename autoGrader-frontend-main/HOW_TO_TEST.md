# 🧪 دليل التجربة السريع

## ✅ الخطوة 1: تشغيل المشروع

افتح Terminal في مجلد المشروع واكتب:

```bash
cd packages/webapp
npm run dev
```

انتظر حتى ترى رسالة مثل:
```
✓ Ready in 3.5s
○ Local:   http://localhost:3000
```

---

## ✅ الخطوة 2: افتح المتصفح

افتح أحد هذه الروابط في المتصفح:

### 🎯 الصفحات المتاحة:

1. **محرك التقييم الدفعي** (الجديد! ⭐)
   ```
   http://localhost:3000/batch-grader
   ```

2. **أداة JSON**
   ```
   http://localhost:3000/json-tool
   ```

3. **محلل JSON بالذكاء الاصطناعي**
   ```
   http://localhost:3000/json-analyzer
   ```

4. **مقارنة JSON**
   ```
   http://localhost:3000/json-compare
   ```

5. **Smart Grader** (الأصلي)
   ```
   http://localhost:3000/smart-grader
   ```

---

## ✅ الخطوة 3: جرب محرك التقييم الدفعي

### الطريقة 1: استخدام ملف JSON

1. افتح: `http://localhost:3000/batch-grader`
2. اضغط على منطقة الرفع
3. اختر ملف: `test-data-example.json`
4. (اختياري) عدّل الإعدادات:
   - الطلبات المتزامنة: 3 (افتراضي)
   - التأخير: 2 ثانية (افتراضي)
   - الحد الأقصى: 20 عنصر (افتراضي)
5. اضغط "بدء التقييم"
6. راقب شريط التقدم
7. راجع النتائج والإحصائيات
8. اضغط "تحميل CSV" لتنزيل النتائج

### الطريقة 2: استخدام ملف CSV

1. افتح: `http://localhost:3000/batch-grader`
2. اضغط على منطقة الرفع
3. اختر ملف: `test-data-example.csv`
4. اضغط "بدء التقييم"
5. راجع النتائج

---

## ✅ الخطوة 4: جرب محلل JSON بالذكاء الاصطناعي

1. افتح: `http://localhost:3000/json-analyzer`
2. ارفع ملف `test-data-example.json`
3. اختر نوع التحليل:
   - 🏗️ تحليل البنية
   - ✅ التحقق من الصحة
   - 💎 جودة البيانات
   - 🔒 الأمان
   - ⚡ الأداء
   - 📋 المخطط
   - 🔍 الأنماط والرؤى
   - ⚙️ تحليل مخصص
4. اضغط "تشغيل التحليل"
5. راجع النتائج والتقييم

---

## ✅ الخطوة 5: جرب أداة JSON

1. افتح: `http://localhost:3000/json-tool`
2. الصق محتوى `test-data-example.json` في المربع
3. جرب العمليات:
   - ✨ Format - تنسيق جميل
   - 🗜️ Minify - ضغط
   - ✅ Validate - التحقق
   - 📊 Stats - إحصائيات
   - 🔍 Search - بحث
   - 📋 Keys - استخراج المفاتيح

---

## 🎯 ما يجب أن تراه

### في محرك التقييم الدفعي:

#### أثناء المعالجة:
```
جاري التقييم...
[████████████░░░░░░░░] 60%
جاري تقييم الواجبات...
```

#### بعد الانتهاء:
```
النتائج:
┌─────────┬─────────┐
│ ناجح    │   5     │
│ فاشل    │   0     │
│ المتوسط │  82     │
│ المجموع │   5     │
└─────────┴─────────┘

معاينة النتائج:
- student_001: 95/100 - واجب ممتاز...
- student_002: 75/100 - جيد لكن يحتاج...
- student_003: 98/100 - متميز جداً...
- student_004: 45/100 - يحتاج تحسين...
- student_005: 88/100 - جيد جداً...
```

---

## 🔧 إعدادات التجربة

### للتجربة السريعة:
```typescript
{
    maxConcurrent: 5,        // 5 طلبات
    delayBetweenRequests: 1, // ثانية واحدة
    maxItems: 5              // 5 عناصر فقط
}
```

### للتجربة الآمنة (حاسوب ضعيف):
```typescript
{
    maxConcurrent: 1,        // طلب واحد
    delayBetweenRequests: 3, // 3 ثواني
    maxItems: 3              // 3 عناصر فقط
}
```

---

## 📊 ملفات التجربة المتوفرة

### 1. test-data-example.json
- 5 واجبات طلاب
- موضوع: الفوتوسينثيسيس
- مستويات مختلفة (ممتاز، جيد، ضعيف)

### 2. test-data-example.csv
- نفس البيانات بتنسيق CSV
- سهل التعديل في Excel

---

## 🎨 ما يمكنك تجربته

### 1. تجربة الإعدادات المختلفة
```
- غيّر عدد الطلبات المتزامنة (1-5)
- غيّر التأخير (1-10 ثواني)
- غيّر الحد الأقصى (1-50)
```

### 2. تجربة أنواع التحليل المختلفة
```
- تحليل البنية
- فحص الأمان
- تقييم الأداء
- تحليل الجودة
```

### 3. تجربة العمليات المختلفة
```
- تنسيق JSON
- البحث في JSON
- مقارنة JSON
- تحويل CSV
```

---

## 🐛 حل المشاكل

### المشكلة: الصفحة لا تفتح
```bash
# تأكد من أن المشروع يعمل
cd packages/webapp
npm run dev
```

### المشكلة: "API key not configured"
```bash
# تحقق من وجود API key
cat .env.local
# يجب أن ترى: GROQ_API_KEY=...
```

### المشكلة: التقييم بطيء
```
# قلل عدد العناصر في الإعدادات
maxItems: 3
```

### المشكلة: خطأ في التحليل
```
# تأكد من صحة تنسيق JSON/CSV
# استخدم json-tool للتحقق
```

---

## 📹 فيديو توضيحي (خطوات)

### 1. تشغيل المشروع (30 ثانية)
```bash
cd packages/webapp
npm run dev
# انتظر "Ready"
```

### 2. فتح الصفحة (10 ثواني)
```
افتح: http://localhost:3000/batch-grader
```

### 3. رفع الملف (10 ثواني)
```
اسحب test-data-example.json
أو اضغط واختر الملف
```

### 4. بدء التقييم (5 ثواني)
```
اضغط "بدء التقييم"
```

### 5. مشاهدة النتائج (30 ثانية)
```
راقب شريط التقدم
راجع الإحصائيات
اضغط "تحميل CSV"
```

**المجموع: دقيقة ونصف! ⚡**

---

## 🎯 النتيجة المتوقعة

بعد التجربة، يجب أن تحصل على:

✅ ملف CSV بالنتائج
✅ إحصائيات شاملة
✅ درجات لكل طالب
✅ ملاحظات مفصلة
✅ نقاط القوة والتحسينات

---

## 🎉 مبروك!

إذا رأيت النتائج، فقد نجحت التجربة! 🎊

الآن يمكنك:
- استخدام ملفاتك الخاصة
- تعديل الإعدادات حسب حاسوبك
- تجربة جميع الصفحات
- استكشاف الميزات المتقدمة

---

## 📞 هل تحتاج مساعدة؟

راجع:
- 📖 `BATCH_GRADING_GUIDE.md` - الدليل الكامل
- 🔄 `N8N_VS_INTEGRATED_COMPARISON.md` - المقارنة
- 📊 `BATCH_GRADING_SUMMARY.md` - الملخص

**استمتع بالتجربة! 🚀**
