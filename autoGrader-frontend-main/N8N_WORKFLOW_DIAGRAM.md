# رسم توضيحي: تدفق n8n مع AutoGrader

## 🔄 التدفق الكامل

```
┌─────────────────────────────────────────────────────────────────┐
│                         n8n Platform                            │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  1. إنشاء Workflow                                       │   │
│  │     ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐              │   │
│  │     │Start│──▶│HTTP │──▶│Func │──▶│ End │              │   │
│  │     └─────┘   └─────┘   └─────┘   └─────┘              │   │
│  │                                                          │   │
│  │  2. اختبار Workflow                                     │   │
│  │  3. تصدير (Download) → my-workflow.json                 │   │
│  └──────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ تحميل ملف JSON
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      AutoGrader Project                         │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  src/lib/n8n/workflows/                                  │  │
│  │    ├── my-workflow.json          ◀── ضع الملف هنا      │  │
│  │    ├── grade-assignments.json                           │  │
│  │    └── student-analytics.json                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              │ ربط بـ Task                      │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  Task Manager (dashboard/index.tsx)                      │  │
│  │                                                          │  │
│  │  tasks = [                                               │  │
│  │    {                                                     │  │
│  │      id: 1,                                              │  │
│  │      title: 'Grade Assignments',                         │  │
│  │      workflowFile: 'my-workflow.json',  ◀── الربط       │  │
│  │      workflowEnabled: true                               │  │
│  │    }                                                     │  │
│  │  ]                                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## 🎬 سيناريو التنفيذ

```
المستخدم                Dashboard              API                n8n
   │                       │                    │                  │
   │  1. يختار Task        │                    │                  │
   ├──────────────────────▶│                    │                  │
   │                       │                    │                  │
   │  2. يكتب رسالة        │                    │                  │
   ├──────────────────────▶│                    │                  │
   │                       │                    │                  │
   │                       │ 3. POST /api/n8n/execute             │
   │                       ├───────────────────▶│                  │
   │                       │                    │                  │
   │                       │                    │ 4. تحميل workflow│
   │                       │                    ├─────────┐        │
   │                       │                    │         │        │
   │                       │                    │◀────────┘        │
   │                       │                    │                  │
   │                       │                    │ 5. تنفيذ (محلي أو API)
   │                       │                    ├─────────────────▶│
   │                       │                    │                  │
   │                       │                    │ 6. النتيجة       │
   │                       │                    │◀─────────────────┤
   │                       │                    │                  │
   │                       │ 7. Response        │                  │
   │                       │◀───────────────────┤                  │
   │                       │                    │                  │
   │  8. عرض النتيجة       │                    │                  │
   │◀──────────────────────┤                    │                  │
   │                       │                    │                  │
```

## 🏗️ بنية الملفات

```
autoGrader-frontend-main/
│
├── packages/webapp/
│   │
│   ├── src/
│   │   ├── lib/
│   │   │   └── n8n/
│   │   │       ├── types.ts              ← تعريفات
│   │   │       ├── WorkflowExecutor.ts   ← محرك التنفيذ
│   │   │       ├── workflows/            ← workflows من n8n
│   │   │       │   ├── grade-assignments.json
│   │   │       │   ├── generate-rubric.json
│   │   │       │   └── my-workflow.json  ← workflow جديد
│   │   │       └── README.md
│   │   │
│   │   └── pages/
│   │       ├── dashboard/
│   │       │   └── index.tsx             ← Task Manager
│   │       │
│   │       └── api/
│   │           └── n8n/
│   │               └── execute.ts        ← API endpoint
│   │
│   ├── .env.local                        ← متغيرات البيئة
│   └── .env.n8n.example                  ← مثال
│
└── Documentation/
    ├── N8N_INTEGRATION_GUIDE.md          ← دليل شامل
    ├── TASK_N8N_EXAMPLE.md               ← مثال عملي
    └── N8N_QUICK_START_AR.md             ← بداية سريعة
```

## 🔀 خيارات التنفيذ

### الخيار 1: تنفيذ محلي (Local Simulation)

```
Task → API → WorkflowExecutor → معالجة محلية → نتيجة
                    │
                    └─ يقرأ JSON
                    └─ ينفذ منطق مبسط
                    └─ لا يحتاج n8n server
```

### الخيار 2: تنفيذ عبر n8n API

```
Task → API → WorkflowExecutor → n8n Server → تنفيذ كامل → نتيجة
                    │                │
                    │                └─ جميع nodes
                    │                └─ جميع integrations
                    │                └─ error handling
                    │
                    └─ يستدعي n8n API
```

### الخيار 3: Webhooks

```
Task → تفعيل webhook → n8n يستقبل → معالجة → callback → نتيجة
         │                  │
         │                  └─ async processing
         │                  └─ عمليات طويلة
         │
         └─ POST إلى n8n webhook URL
```

## 📊 مثال Workflow: تقييم واجب

```
┌─────────────────────────────────────────────────────────────┐
│                    Grade Assignment Workflow                │
└─────────────────────────────────────────────────────────────┘

  ┌─────────┐
  │  Start  │  ← استقبال بيانات الواجب
  └────┬────┘
       │
       ▼
  ┌─────────────────┐
  │ HTTP Request    │  ← استدعاء AI API (Groq/OpenAI)
  │ POST /api/groq  │
  └────┬────────────┘
       │
       ▼
  ┌─────────────────┐
  │ Function Node   │  ← معالجة النتيجة
  │ Calculate Grade │     - استخراج الدرجة
  └────┬────────────┘     - تنسيق الملاحظات
       │
       ▼
  ┌─────────────────┐
  │ Set Node        │  ← تجهيز البيانات للحفظ
  │ Format Output   │
  └────┬────────────┘
       │
       ▼
  ┌─────────────────┐
  │ HTTP Request    │  ← حفظ في قاعدة البيانات
  │ POST /api/save  │
  └────┬────────────┘
       │
       ▼
  ┌─────────────────┐
  │ Email Node      │  ← إرسال إشعار للطالب (اختياري)
  └─────────────────┘
```

## 🎯 حالات الاستخدام

### 1. تقييم تلقائي

```
طالب يرسل واجب → Task Manager → n8n Workflow
                                      │
                                      ├─ AI Analysis
                                      ├─ Grade Calculation
                                      ├─ Feedback Generation
                                      └─ Save & Notify
```

### 2. تحليل الأداء

```
مدرس يطلب تحليل → Task Manager → n8n Workflow
                                      │
                                      ├─ Fetch Student Data
                                      ├─ Statistical Analysis
                                      ├─ Identify At-Risk
                                      └─ Generate Report
```

### 3. إنشاء Rubric

```
مدرس يطلب rubric → Task Manager → n8n Workflow
                                      │
                                      ├─ AI Prompt
                                      ├─ Generate Criteria
                                      ├─ Format Structure
                                      └─ Save Template
```

## 🔐 الأمان

```
┌──────────────────────────────────────────────────────────┐
│  Security Layers                                         │
│                                                          │
│  1. Environment Variables                                │
│     └─ API Keys في .env.local                           │
│                                                          │
│  2. API Authentication                                   │
│     └─ N8N_API_KEY للوصول                               │
│                                                          │
│  3. Input Validation                                     │
│     └─ التحقق من البيانات المدخلة                       │
│                                                          │
│  4. Error Handling                                       │
│     └─ معالجة الأخطاء بشكل آمن                          │
└──────────────────────────────────────────────────────────┘
```
