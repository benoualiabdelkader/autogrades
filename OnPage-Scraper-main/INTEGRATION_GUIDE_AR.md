# 🚀 دليل التكامل الكامل - AutoGrader Smart Scraper Extension

## 📋 نظرة عامة

تم تطوير Extension احترافي يتكامل بشكل كامل مع مشروع AutoGrader لاستخراج البيانات من صفحات الويب بذكاء وإرسالها مباشرة إلى Dashboard.

---

## ✨ المميزات الرئيسية

### 1. **استخراج ذكي للبيانات (Smart DOM Extraction)**
- ✅ تحليل ذكي لعناصر DOM
- ✅ استخراج تلقائي للحقول (inputs, textareas, selects)
- ✅ كشف تلقائي لأنواع البيانات
- ✅ استخراج البيانات الوصفية (metadata)

### 2. **JSON منظم ونظيف**
- ✅ تنسيق JSON احترافي
- ✅ تجميع البيانات حسب الحقول
- ✅ إحصائيات شاملة
- ✅ معلومات وصفية كاملة

### 3. **تكامل مع AutoGrader**
- ✅ إرسال مباشر إلى Dashboard
- ✅ عرض البيانات بشكل احترافي
- ✅ تصدير CSV و JSON
- ✅ حفظ تلقائي للبيانات

### 4. **واجهة مستخدم عصرية**
- ✅ تصميم Dark Mode احترافي
- ✅ واجهة عربية كاملة (RTL)
- ✅ رسوم متحركة سلسة
- ✅ تجربة مستخدم ممتازة

### 5. **لا يعمل تلقائياً**
- ✅ يتطلب نقرة زر للتشغيل
- ✅ لا يوجد scraping عدواني
- ✅ احترام خصوصية المستخدم
- ✅ تحكم كامل في العملية

---

## 🏗️ البنية المعمارية

```
OnPage-Scraper-Extension/
├── smart-extractor.js          # محرك الاستخراج الذكي
├── autograder-integration.js   # وحدة التكامل مع AutoGrader
├── enhanced-popup.html         # واجهة المستخدم
├── enhanced-popup.css          # التصميم
├── enhanced-popup.js           # منطق التحكم
├── content.js                  # سكريبت المحتوى
└── manifest.json               # إعدادات Extension

AutoGrader-Project/
├── src/pages/api/
│   ├── scraper-data.ts        # API لاستقبال البيانات
│   └── health.ts              # فحص الصحة
└── src/components/
    └── ScraperDataDashboard.tsx  # لوحة عرض البيانات
```

---

## 📦 التثبيت والإعداد

### 1. تثبيت Extension

```bash
# 1. افتح Chrome
chrome://extensions/

# 2. فعّل وضع المطور (Developer mode)

# 3. اضغط "Load unpacked"

# 4. اختر مجلد: OnPage-Scraper-main/extension
```

### 2. إعداد AutoGrader

```bash
# 1. انتقل إلى مجلد المشروع
cd autoGrader-frontend-main/autoGrader-frontend-main/packages/webapp

# 2. شغّل المشروع
npm run dev

# 3. افتح المتصفح على
http://localhost:5173
```

---

## 🎯 كيفية الاستخدام

### الطريقة 1: التحديد اليدوي

1. **افتح الصفحة المراد استخراج بياناتها**
2. **اضغط على أيقونة Extension**
3. **اضغط "تحديد العناصر يدوياً"**
4. **حدد العناصر بالنقر عليها**
5. **اضغط "Finish" عند الانتهاء**
6. **اضغط "استخراج وإرسال إلى AutoGrader"**

### الطريقة 2: الاستخراج التلقائي

1. **افتح الصفحة المراد استخراج بياناتها**
2. **اضغط على أيقونة Extension**
3. **اضغط "استخراج تلقائي"**
4. **انتظر حتى يتم الاستخراج**
5. **اضغط "استخراج وإرسال إلى AutoGrader"**

---

## 📊 تنسيق البيانات المُرسلة

### مثال على JSON المُرسل:

```json
{
  "source": "web-scraper",
  "timestamp": "2026-02-25T10:30:00.000Z",
  "url": "https://example.com",
  "pageTitle": "Example Page",
  "data": [
    {
      "id": "field_domain_0",
      "fieldName": "field_domain",
      "value": "example.com",
      "type": "input",
      "metadata": {
        "className": "input-field",
        "id": "domain",
        "inputType": "text"
      }
    }
  ],
  "statistics": {
    "totalItems": 10,
    "totalFields": 5,
    "fieldCounts": {
      "field_domain": 2,
      "field_email": 3,
      "field_name": 5
    }
  },
  "metadata": {
    "extractionMethod": "smart-dom-extraction",
    "userAgent": "Mozilla/5.0...",
    "screenResolution": "1920x1080",
    "language": "ar"
  }
}
```

---

## 🔧 الإعدادات المتقدمة

### تغيير عنوان AutoGrader

```javascript
// في Extension Popup
document.getElementById('autoGraderUrl').value = 'http://localhost:5173';
```

### تخصيص خيارات الاستخراج

```javascript
const extractionOptions = {
    includeInputs: true,      // استخراج الحقول
    includeText: true,        // استخراج النصوص
    includeLinks: false,      // استخراج الروابط
    includeImages: false,     // استخراج الصور
    containerSelector: 'body' // العنصر الحاوي
};
```

---

## 🎨 واجهة Dashboard

### عرض البيانات

```tsx
import ScraperDataDashboard from '@/components/ScraperDataDashboard';

// في أي صفحة
<ScraperDataDashboard />
```

### المميزات:
- ✅ عرض إحصائيات شاملة
- ✅ تبديل بين عرض الجدول و JSON
- ✅ تصدير CSV و JSON
- ✅ تحديث تلقائي
- ✅ مسح البيانات

---

## 🔌 API Endpoints

### 1. استقبال البيانات
```
POST /api/scraper-data
Content-Type: application/json

{
  "source": "web-scraper",
  "data": [...],
  "statistics": {...}
}
```

### 2. فحص الصحة
```
GET /api/health

Response:
{
  "success": true,
  "status": "healthy",
  "service": "AutoGrader API"
}
```

---

## 🛡️ الأمان والخصوصية

### ✅ ممارسات آمنة:
- لا يعمل تلقائياً بدون إذن المستخدم
- لا يرسل بيانات إلا عند الطلب
- يحترم robots.txt
- لا يخزن بيانات حساسة
- CORS محمي

### ⚠️ تحذيرات:
- استخدم فقط على المواقع التي لديك إذن باستخراج بياناتها
- لا تستخرج بيانات شخصية بدون موافقة
- احترم شروط الخدمة للمواقع

---

## 🐛 استكشاف الأخطاء

### المشكلة: Extension لا يعمل
```bash
# الحل:
1. تأكد من تفعيل Developer Mode
2. أعد تحميل Extension
3. أعد تحميل الصفحة
```

### المشكلة: فشل الاتصال بـ AutoGrader
```bash
# الحل:
1. تأكد من تشغيل AutoGrader على localhost:5173
2. اضغط "اختبار الاتصال"
3. تحقق من إعدادات CORS
```

### المشكلة: لا يتم استخراج البيانات
```bash
# الحل:
1. تأكد من تحديد العناصر بشكل صحيح
2. جرب الاستخراج التلقائي
3. افتح Console للتحقق من الأخطاء
```

---

## 📈 الأداء والتحسينات

### ✅ محسّن للأداء:
- استخراج سريع وفعال
- لا يؤثر على أداء الصفحة
- استهلاك منخفض للذاكرة
- معالجة متوازية

### 🚀 التحسينات المستقبلية:
- [ ] دعم استخراج الجداول
- [ ] استخراج متعدد الصفحات
- [ ] جدولة الاستخراج
- [ ] تصدير Excel
- [ ] تكامل مع قواعد البيانات

---

## 🤝 المساهمة

نرحب بمساهماتك! يرجى:
1. Fork المشروع
2. إنشاء فرع جديد
3. إجراء التعديلات
4. إرسال Pull Request

---

## 📄 الترخيص

MIT License - استخدم بحرية مع الإشارة للمصدر

---

## 📞 الدعم

للمساعدة أو الاستفسارات:
- 📧 Email: support@autograder.com
- 💬 Discord: AutoGrader Community
- 📚 Docs: docs.autograder.com

---

## 🎉 شكراً لاستخدامك AutoGrader Smart Scraper!

**Powered by AutoGrader AI • v2.0**
