# ⚡ دليل البدء السريع - AutoGrader Smart Scraper

## 🎯 في 3 خطوات فقط!

### الخطوة 1: تثبيت Extension ⬇️

```bash
1. افتح Chrome
2. اذهب إلى: chrome://extensions/
3. فعّل "Developer mode" (أعلى اليمين)
4. اضغط "Load unpacked"
5. اختر مجلد: OnPage-Scraper-main/extension
```

✅ **تم! Extension جاهز للاستخدام**

---

### الخطوة 2: تشغيل AutoGrader 🚀

```bash
cd autoGrader-frontend-main/autoGrader-frontend-main/packages/webapp
npm run dev
```

✅ **AutoGrader يعمل على: http://localhost:5173**

---

### الخطوة 3: استخراج البيانات 📊

#### طريقة سريعة (استخراج تلقائي):

1. افتح أي صفحة ويب
2. اضغط على أيقونة Extension
3. اضغط **"استخراج تلقائي"**
4. اضغط **"استخراج وإرسال إلى AutoGrader"**

✅ **تم! البيانات الآن في Dashboard**

#### طريقة متقدمة (تحديد يدوي):

1. افتح أي صفحة ويب
2. اضغط على أيقونة Extension
3. اضغط **"تحديد العناصر يدوياً"**
4. انقر على العناصر التي تريد استخراجها
5. اضغط **"Finish"**
6. اضغط **"استخراج وإرسال إلى AutoGrader"**

✅ **تم! البيانات المحددة في Dashboard**

---

## 🎨 عرض البيانات في Dashboard

### إضافة Dashboard إلى صفحتك:

```tsx
import ScraperDataDashboard from '@/components/ScraperDataDashboard';

export default function MyPage() {
    return (
        <div>
            <h1>بيانات الاستخراج</h1>
            <ScraperDataDashboard />
        </div>
    );
}
```

---

## 🔥 مثال عملي كامل

### السيناريو: استخراج بيانات نموذج

```javascript
// 1. افتح صفحة تحتوي على نموذج
// مثال: https://example.com/form

// 2. اضغط Extension → "استخراج تلقائي"

// 3. النتيجة المتوقعة:
{
  "totalItems": 5,
  "fields": {
    "name": ["محمد أحمد"],
    "email": ["mohamed@example.com"],
    "phone": ["0123456789"],
    "message": ["رسالة تجريبية"]
  }
}

// 4. البيانات تظهر فوراً في AutoGrader Dashboard
```

---

## ⚙️ الإعدادات الأساسية

### تغيير عنوان AutoGrader:

1. افتح Extension Popup
2. اذهب إلى قسم "الإعدادات"
3. غيّر العنوان إلى عنوان خادمك
4. اضغط "اختبار الاتصال"

```
الافتراضي: http://localhost:5173
الإنتاج: https://your-domain.com
```

---

## 📤 تصدير البيانات

### تصدير CSV:
```
Extension → معاينة البيانات → تصدير CSV
```

### تصدير JSON:
```
Extension → معاينة البيانات → تصدير JSON
```

---

## 🐛 حل المشاكل السريع

### ❌ Extension لا يظهر؟
```bash
✅ تأكد من تفعيل Developer Mode
✅ أعد تحميل Extension
✅ أعد تشغيل Chrome
```

### ❌ فشل الاتصال؟
```bash
✅ تأكد من تشغيل AutoGrader
✅ تحقق من العنوان: http://localhost:5173
✅ اضغط "اختبار الاتصال"
```

### ❌ لا يتم استخراج البيانات؟
```bash
✅ أعد تحميل الصفحة
✅ جرب الاستخراج التلقائي
✅ تحقق من Console للأخطاء
```

---

## 🎓 نصائح احترافية

### ✨ للحصول على أفضل النتائج:

1. **استخدم الاستخراج التلقائي** للصفحات البسيطة
2. **استخدم التحديد اليدوي** للصفحات المعقدة
3. **فعّل "تنظيف البيانات"** لنتائج أنظف
4. **فعّل "إزالة التكرارات"** لتجنب البيانات المكررة
5. **استخدم "معاينة البيانات"** قبل الإرسال

---

## 📊 أمثلة الاستخدام

### مثال 1: استخراج قائمة منتجات
```
1. افتح صفحة المنتجات
2. استخراج تلقائي
3. النتيجة: أسماء وأسعار المنتجات
```

### مثال 2: استخراج بيانات جدول
```
1. افتح صفحة تحتوي على جدول
2. تحديد يدوي للخلايا
3. النتيجة: بيانات الجدول منظمة
```

### مثال 3: استخراج نماذج اتصال
```
1. افتح صفحة نموذج
2. استخراج تلقائي
3. النتيجة: جميع حقول النموذج
```

---

## 🚀 الخطوات التالية

بعد إتقان الأساسيات:

1. 📖 اقرأ [دليل التكامل الكامل](INTEGRATION_GUIDE_AR.md)
2. 🎨 خصص Dashboard حسب احتياجاتك
3. 🔧 استكشف الإعدادات المتقدمة
4. 💡 شارك تجربتك مع المجتمع

---

## 💬 هل تحتاج مساعدة؟

- 📚 الوثائق الكاملة: [INTEGRATION_GUIDE_AR.md](INTEGRATION_GUIDE_AR.md)
- 🐛 الإبلاغ عن مشكلة: GitHub Issues
- 💡 اقتراحات: GitHub Discussions

---

## 🎉 مبروك!

أنت الآن جاهز لاستخدام **AutoGrader Smart Scraper** بكفاءة!

**Happy Scraping! 🚀**

---

**Powered by AutoGrader AI • v2.0**
